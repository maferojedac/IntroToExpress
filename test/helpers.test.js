// import {describe, expect, it } from '@jest/globals'

describe('Helpers', () => { 
  it('Tests true', () => {
    const hello = 2;
    expect(hello).toBe(2);
  });
  it('Tests false', () => {
    const hello = 2;
    expect(hello).toBe(2);
  });
 });